## Лабораторная работа 1
1)Что означает аббревиатура CRUD?
2)Какие четыре операции включает в себя CRUD?
3)Почему CRUD является типовой задачей в веб-разработке?
4)Что представляет собой REST API в контексте CRUD?
5)Чем отличаются операции PUT и PATCH в REST API?
6)Как в Spring Boot обозначается класс-сущность для базы данных?
7)Для чего используется аннотация @Entity?
8)Какую роль играет аннотация @Id в сущности?
9)Что делает аннотация @GeneratedValue(strategy = GenerationType.IDENTITY)?
10)Как связать сущности Post и User в JPA?
11)Зачем нужны DTO-классы?
12)Чем отличается PostCreateDTO от PostUpdateDTO?
13)Что такое JsonNullable и зачем его используют в DTO для обновления?
14)Для чего применяется библиотека MapStruct?
15)В чем преимущество MapStruct по сравнению с ручным маппингом?
16)Как в MapStruct можно обновить только измененные поля сущности?
17)Что представляет собой интерфейс JpaRepository?
18)Какие базовые методы CRUD предоставляет JpaRepository?
19)Как написать метод для поиска поста по slug в репозитории?
20)Что возвращает метод findBySlug(String slug) в репозитории?
21)Что такое TDD (Test Driven Development)?
22)Чем интеграционные тесты отличаются от модульных?
23)Для чего в тестах используется MockMvc?
24)Как проверить статус ответа API в тесте?
25)Как в тестах проверить, что объект действительно был сохранен в базе данных?
26)Для чего предназначена библиотека Lombok?
27)Что делают аннотации @Getter и @Setter?
28)Чем отличаются @NoArgsConstructor, @AllArgsConstructor и @RequiredArgsConstructor?
29)Что делает аннотация @Data?
30)Чем отличается @Data от @Value?
![[МА ЛБ 1 (2).pdf]]
## Лабораторная работа 2
Требования к лаб работе.  
  
```md  
Уровень 1  
Оценка 4 - 5 (Выполнить все что требуется по методичке, вопросов не будет)  
```  
```md  
Уровень 2  
Оценка 6-7 (Выполнить все что требуется по методичке + Ответить на 2 вопроса)  
```  
```md  
Уровень 3  
Оценка 8 - 10 (Выбрать любой вариант и выполнить задание + Ответить на 1 вопрос)  
```  
```md  
Вопросы  
  
1) Что такое юнит-тестирование и зачем его применять в Spring Boot-проектах?  
  
2) Какие библиотеки входят в spring-boot-starter-test и для чего они нужны (JUnit, Mockito, AssertJ/Hamcrest)?  
  
3) Чем отличаются юнит-тесты от интеграционных и сквозных тестов в контексте Spring Boot?  
  
4) Для какой цели в тестах используют AssertJ, и чем он удобнее “классических” assert?  
  
5) Почему рекомендуется разделять “боевую” БД и тестовую, и зачем использовать H2 в тестах?  
  
6) Какие поля и аннотации содержит сущность Employee, почему email обязателен?  
  
7) Какую дополнительную операцию кроме CRUD реализует EmployeeRepository (например, findByEmail), и что она возвращает?  
  
8) Зачем в тестах репозитория используют @DataJpaTest, что оно настраивает автоматически?  
  
9) Для чего иногда применяют @Rollback(false) и @Order(n) в тестах репозитория, как это влияет на данные/порядок?  
  
10) Какие базовые сценарии должен покрывать тест репозитория для CRUD (save/get/findAll/update/delete)?  
  
11) Зачем сервису проверять уникальность email при сохранении сотрудника и какое поведение ожидается при нарушении?  
  
12) Каков смысл аннотаций @ExtendWith(MockitoExtension.class), @Mock и @InjectMocks в тесте сервиса?  
  
13) Чем отличается given(...).willReturn(...) от willDoNothing() в BDD-стиле Mockito? Приведите пример.  
  
14) Какие взаимодействия с репозиторием следует верифицировать в тесте метода deleteEmployee?  
  
15) Как проверить в тестах сервиса, что updateEmployee изменяет нужные поля и возвращает обновлённую сущность?  
  
16) Для чего используется @WebMvcTest(EmployeeController.class) и почему в нём применяют @MockBean для сервиса?  
  
17) Что такое MockMvc и какую задачу он решает при тестировании контроллеров Spring MVC?  
  
18) Как в тесте POST-эндпоинта проверить статус 201 Created и содержание JSON-тела?  
  
19) Как протестировать GET-эндпоинт, возвращающий список, и сверить размер массива через JSONPath?  
  
20) Как в тесте PUT-эндпоинта смоделировать существование ресурса и проверить обновлённые поля?  
  
21) Что именно проверяется в тесте DELETE-эндпоинта (статус, взаимодействие с сервисом и т.п.)?  
  
22) Зачем в тестах контроллера использовать ObjectMapper, где и как он применяется?  
  
23) Какие ключевые параметры указываются в application.properties для “боевой” БД, и какие — для тестовой H2?  
  
24) Почему использование реальной БД в юнит-тестах может портить данные, и как этого избежать?  
  
25) В чём разница подходов: тесты репозитория на реальном JpaRepository vs тесты сервиса/контроллера с моками?  
  
26) Для чего используют @BeforeEach и как она помогает сократить дублирование кода в тестах?  
  
27) Как работает упорядочивание тестов через @TestMethodOrder и @Order, и когда это уместно?  
  
28) Какие типичные проверки выполняются утверждениями AssertJ в примерах ЛР?  
  
29) Какие негативные сценарии стоит добавить (дубликат email, запрос несуществующего id, валидация полей) и где их лучше тестировать — в сервисе, контроллере или обоих слоях?  
  
30) Если тесты контроллера падают из-за несоответствия JSON-структуры, какие шаги диагностики выполните в первую очередь (моки сервиса, сериализация, пути JSONPath, статус-коды)?  
```